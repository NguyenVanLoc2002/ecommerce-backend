-- =====================================================
-- V1__init_schema.sql
-- Baseline migration — schema version 1
-- Auth, User, Catalog, Inventory, Order tables
-- will be added in subsequent migrations (V2, V3, ...)
-- =====================================================

-- Flyway baseline: schema initialized
SELECT 1;
